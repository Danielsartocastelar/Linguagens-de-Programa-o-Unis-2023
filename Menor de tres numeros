# Solicita os três números inteiros
numero1 = int(input("Digite o primeiro número: "))
numero2 = int(input("Digite o segundo número: "))
numero3 = int(input("Digite o terceiro número: "))

# Inicializa o menor como o primeiro número
menor = numero1

# Verifica se o segundo número é menor do que o atual menor
if numero2 < menor:
    menor = numero2

# Verifica se o terceiro número é menor do que o atual menor
if numero3 < menor:
    menor = numero3

# Exibe o menor número
print(f"O menor número é: {menor}")
